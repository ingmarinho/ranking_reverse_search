rrs TODO — grouped for focused brainstorming sessions.
Each "GROUP" below is meant to be one brainstorming/design session.

==============================================================================
GROUP A — Frame-picker & cropping UX  [IN PROGRESS: being designed]
==============================================================================
- F1: fix scaling of image inside frame picker
- F2: improve cropping — allow dragging the selected crop, dragging by
      corners/sides, and make it smoother
- F5: fix source URL bar — align placeholder text better with size of box,
      center it maybe?

==============================================================================
GROUP B — Download management  [DONE]
==============================================================================
- F3: figure out a convenient path to store downloaded files
- F6: add an extra clip downloader at the bottom which can be used to download
      any (extra) clip
- F7: make sure that if a clip with the exact same title is used twice, it
      won't cause issues or overwrite previous downloads for a video with the
      same title that was used in the past

==============================================================================
GROUP C — imgbb key & onboarding
==============================================================================
- F4: allow setting the imgbb API key inside the webapp somewhere instead of
      having to supply it beforehand. Make it easier to use. Make the software
      unusable without an imgbb key. Save the key in the browser (cookie /
      localStorage) or on disk in a file.

==============================================================================
GROUP D — Bugs (likely direct fixes, not design)
==============================================================================
- B1: index error selecting the last frame of the final scene of a video.
      Example:
        frame 656 / scene 521–657
        could not read frame 657 from
        /Users/ingmar/Dev/Repos/ranking_reverse_search/data/jobs/16/source.mp4

- B2: deno not being detected at all by yt-dlp → 403s / missing formats.
      Warnings seen:
        WARNING: [youtube] [jsc] Remote components challenge solver script
        (deno) and NPM package (deno) were skipped. These may be required to
        solve JS challenges. You can enable these downloads with
        --remote-components ejs:github (recommended) or --remote-components
        ejs:npm, respectively. See https://github.com/yt-dlp/yt-dlp/wiki/EJS
        WARNING: [youtube] m8511B4tXZk: n challenge solving failed: Some
        formats may be missing. Ensure you have a supported JavaScript runtime
        and challenge solver script distribution installed.
        ERROR: unable to download video data: HTTP Error 403: Forbidden

- B3: intermittent ASGI error (everything still worked, but strange):
        RuntimeError: Response content shorter than Content-Length
      Raised from starlette/uvicorn during a NiceGUI response
      (nicegui _exception_handler_500 → starlette base middleware send).
